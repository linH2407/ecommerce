import React from "react";

const HeaderName = ({ name }) => {
  return (
    <div className="text-[#6A983C] text-[12px] font-normal">
      <p>{name}</p>
    </div>
  );
};

export default HeaderName;
